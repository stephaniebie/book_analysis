def bfs(root, target):
    """
    Searches for a target within a tree using the breadth-first search algorithm.

    Parameters
    ----------
    root
        Root node within a tree
    target
        Target to match with a node within the tree. If the node matches this target, returns the node object. Else returns None

    Returns
    -------
    Node that matches the target. If not found, returns None.
    """
    raise NotImplementedError


def dfs(root, target):
    """
    Searches for a target within a tree using the depth-first search algorithm.

    Parameters
    ----------
    root
        Root node within a tree
    target
        Target to match with a node within the tree. If the node matches this target, returns the node object. Else returns None

    Returns
    -------
    Node that matches the target. If not found, returns None.
    """
    raise NotImplementedError
